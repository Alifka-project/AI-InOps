# Runtime dependencies only (what the deployed backend imports).
# Kept lean so the service fits serverless size limits. Dev/CI extras
# (scipy for verify.py, pytest, linters) live in requirements-dev.txt.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9
numpy==1.26.4
pandas==2.2.2
openpyxl==3.1.4
reportlab==4.2.0
matplotlib==3.9.0
